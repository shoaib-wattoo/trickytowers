using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class ShapeTest
{
    /*
    [UnityTest]
    public IEnumerator Shape_Right_Movement_Test()
    {
        var gameObject = new GameObject();
        Vector3 startPos = gameObject.transform.position;
        var shaep = gameObject.AddComponent<TrickyShape>();
        shaep.MoveHorizontal(Vector2.right);
        yield return new WaitForEndOfFrame();

        Assert.AreEqual(new Vector3(startPos.x + 0.5f, startPos.y, startPos.z), gameObject.transform.position);
    }

    [UnityTest]
    public IEnumerator Shape_Left_Movement_Test()
    {
        var gameObject = new GameObject();
        Vector3 startPos = gameObject.transform.position;
        var shaep = gameObject.AddComponent<TrickyShape>();
        shaep.MoveHorizontal(Vector2.left);
        yield return new WaitForEndOfFrame();

        Assert.AreEqual(new Vector3(startPos.x + 0.5f, startPos.y, startPos.z), gameObject.transform.position);
    }

    [UnityTest]
    public IEnumerator Shape_Rotation_Test()
    {
        var gameObject = new GameObject();
        Quaternion startRotation = gameObject.transform.rotation;
        var shaep = gameObject.AddComponent<TrickyShape>();
        shaep.RotateClockWise(true);
        yield return new WaitForEndOfFrame();
        float angle = Quaternion.Angle(startRotation, gameObject.transform.rotation);

        Assert.AreEqual(angle,90f);
    }
    */
}
